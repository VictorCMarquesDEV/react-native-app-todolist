export type TaskDatabase = {
    idtask: number
    nametask: string
    statustask: number
    prioritytask: number
}

export type TaskItemDatabase = {
    idtaskitem: number
    nametaskitem: string
}
